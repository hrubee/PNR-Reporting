generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("database_DATABASE_URL")
}

// Role: "ADMIN" | "EMPLOYEE"
// Sheet: "HYGIENE_REPORT" | "GLASS_REPORT" | "FRIDGE_REPORT" | "KITCHEN" | "PRODUCTION" | "PUFF_ROOM" | "CAKE_ROOM"

model User {
  id           String   @id @default(cuid())
  name         String
  email        String   @unique
  passwordHash String
  role         String   @default("EMPLOYEE")
  outletId     String?  // "bakery" | "oreta-world" | "all"
  jobTitle     String?  // e.g. "Prep Chef", "Cleaner", "Supervisor"
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  sheetAccess       SheetAccess[]
  hygieneEntries    HygieneEntry[]
  glassEntries      GlassEntry[]
  fridgeEntries     FridgeEntry[]
  kitchenEntries    KitchenEntry[]
  productionEntries ProductionEntry[]
  puffRoomEntries   PuffRoomEntry[]
  cakeRoomEntries       CakeRoomEntry[]
  oretaHygieneEntries   OretaHygieneEntry[]
  oretaEquipmentEntries OretaEquipmentEntry[]
  oretaFridgeEntries    OretaFridgeEntry[]
  oretaGlassEntries     OretaGlassEntry[]
  oretaMonthlyEntries   OretaMonthlyEntry[]
  oretaFoodEntries      OretaFoodEntry[]
}

model SheetAccess {
  id        String   @id @default(cuid())
  userId    String
  sheet     String
  grantedAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, sheet])
}

// ─── HYGIENE REPORT ───────────────────────────────────────────────────────────
model HygieneEntry {
  id            String   @id @default(cuid())
  date          String
  day           String
  submittedById String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  areaChecks       String
  supervisorName   String @default("")
  comments         String @default("")
  correctiveAction String @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── GLASS REPORT ─────────────────────────────────────────────────────────────
model GlassEntry {
  id            String   @id @default(cuid())
  date          String
  submittedById String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  locationChecks   String
  supervisorName   String @default("")
  comments         String @default("")
  correctiveAction String @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── FRIDGE REPORT ────────────────────────────────────────────────────────────
model FridgeEntry {
  id            String   @id @default(cuid())
  date          String
  supervisedBy  String   @default("")
  submittedById String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  fridgeChecks     String
  hygiene          String @default("")
  comments         String @default("")
  correctiveAction String @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── KITCHEN ──────────────────────────────────────────────────────────────────
model KitchenEntry {
  id            String   @id @default(cuid())
  date          String
  submittedById String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  equipmentChecks  String
  supervisorName   String @default("")
  workerName       String @default("")
  comments         String @default("")
  correctiveAction String @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── PRODUCTION ───────────────────────────────────────────────────────────────
model ProductionEntry {
  id            String   @id @default(cuid())
  date          String
  submittedById String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  equipmentChecks  String
  supervisorName   String @default("")
  workerName       String @default("")
  comments         String @default("")
  correctiveAction String @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── PUFF ROOM ────────────────────────────────────────────────────────────────
model PuffRoomEntry {
  id            String   @id @default(cuid())
  date          String
  submittedById String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  equipmentChecks  String
  supervisorName   String @default("")
  workerName       String @default("")
  comments         String @default("")
  correctiveAction String @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── CAKE ROOM ────────────────────────────────────────────────────────────────
model CakeRoomEntry {
  id            String   @id @default(cuid())
  date          String
  submittedById String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  equipmentChecks  String
  supervisorName   String @default("")
  workerName       String @default("")
  comments         String @default("")
  correctiveAction String @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── ORETA WORLD HYGIENE & CLEANING SOP ───────────────────────────────────────
model OretaHygieneEntry {
  id               String   @id @default(cuid())
  date             String
  day              String
  submittedById    String
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  areaChecks       String   // JSON array of 12 areas, each having checks for morning, afternoon, evening, night
  supervisorName   String   @default("")
  comments         String   @default("")
  correctiveAction String   @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── ORETA WORLD EQUIPMENT CLEANING ───────────────────────────────────────────
model OretaEquipmentEntry {
  id               String   @id @default(cuid())
  date             String
  submittedById    String
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  equipmentChecks  String   // JSON array of 41 equipment checks (status, time, cleanedBy, checkedBy)
  supervisorName   String   @default("")
  comments         String   @default("")
  correctiveAction String   @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── ORETA WORLD FRIDGE & TEMPERATURE REPORT ──────────────────────────────────
model OretaFridgeEntry {
  id               String   @id @default(cuid())
  date             String
  supervisedBy     String   @default("")
  submittedById    String
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  fridgeChecks     String   // JSON array of 7 fridge/freezer & cake display units
  hygiene          String   @default("")
  comments         String   @default("")
  correctiveAction String   @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── ORETA WORLD GLASS REPORT ─────────────────────────────────────────────────
model OretaGlassEntry {
  id               String   @id @default(cuid())
  date             String
  submittedById    String
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  locationChecks   String   // JSON array of 6 glass panels (Ground Floor 1-4, Mezzanine 1-2)
  supervisorName   String   @default("")
  comments         String   @default("")
  correctiveAction String   @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}

// ─── ORETA WORLD MONTHLY CLEANING & MAINTENANCE ───────────────────────────────
model OretaMonthlyEntry {
  id               String   @id @default(cuid())
  month            String   // YYYY-MM
  date             String
  submittedById    String
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  taskChecks       String   // JSON array of 6 monthly maintenance tasks
  supervisorName   String   @default("")
  comments         String   @default("")
  correctiveAction String   @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([month])
  @@index([date])
}

// ─── ORETA WORLD FOOD SAFETY & PRODUCT LOGS ──────────────────────────────────
model OretaFoodEntry {
  id               String   @id @default(cuid())
  date             String
  day              String   @default("")
  supervisorName   String   @default("")
  submittedById    String
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  tempChecks       String   // JSON array of 3 prep items (Boiled chicken, Spinach, Boiled Potato)
  vegChecks        String   // JSON array of 6 veg fillings & sauces
  nonVegChecks     String   // JSON array of 3 non-veg fillings
  comments         String   @default("")
  correctiveAction String   @default("")

  submittedBy User @relation(fields: [submittedById], references: [id])

  @@index([date])
}



